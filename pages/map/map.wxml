
<!--pages/map/map.wxml-->
<view class="container">
  <map id="map" longitude="{{map.longitude}}" latitude="{{map.latitude}}" scale="{{map.scale}}" controls="{{controls}}" bindcontroltap="controltap" markers="{{markers}}" bindmarkertap="markertap" polyline="{{polyline}}" bindregionchange="regionchange"  bindtap="getMap" show-location style="width: 100%; height: 300px;"></map>
</view>